# EigenGuard Nexus Backend dependencies (Sprint 17 skeleton)
# Python 3.12, FastAPI 0.115+, SQLAlchemy 2.x async, Pydantic v2

fastapi==0.115.6
uvicorn[standard]==0.32.1

# Async ORM stack
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.1

# Config + validation
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# Auth + crypto (license signing, JWT)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==44.0.0

# HTTP client (Superadmin license server check)
httpx==0.28.1

# Cache (license cache + alert pub/sub later)
redis==5.2.1

# Misc
python-multipart==0.0.20
